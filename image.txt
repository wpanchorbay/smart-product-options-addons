# OptionBay Documentation — Required Screenshots
# Save all screenshots to: docs/public/img/
# Format: WebP or PNG, width 1280px recommended
# ─────────────────────────────────────────────────────────────

## builder/option-groups.md
option-groups-list.png
  → The full Option Groups list screen
  → Show the table with several groups, status toggles, filter tabs (All / Published / Draft), search bar, and the "Add new group / Import / Export" header buttons
  → At least 4-5 rows visible, one in Draft, rest Published

option-groups-header.png
  → The top header bar showing the "Option Groups" title, "Add new group" link, "Import" button, and "Export" button
  → All buttons should be in their default (non-active) state

option-groups-filters.png
  → The status filter tabs (All / Published / Draft / Trash) with item counts shown in parentheses
  → The "All" tab should be active/bold

option-groups-row-actions.png
  → A single table row being hovered, showing the inline action links (Edit | Duplicate | Move to Trash)
  → The status toggle switch should also be visible in the Status column

option-groups-columns.png
  → The column picker popover open (⋮ icon at top-right of table)
  → Show all 7 column checkboxes: Fields, Assigned To, Status, Created By, Created At, Updated By, Updated At

option-groups-bulk-confirm.png
  → The confirmation modal open, asking to confirm a bulk action (e.g. "Move to Trash 3 selected items?")
  → Show the red "Confirm" and "Cancel" buttons

option-groups-inventory-btn.png
  → The "View Inventory" button (📦 icon) highlighted in the top-right controls area next to the search box

## builder/addon-builder.md
addon-builder-full.png
  → Full Addon Builder page for a group that has a title, 3+ fields, and assignment rules visible
  → Show the right sidebar with the field type buttons visible
  → Capture the entire viewport (no scrolling)

addon-builder-header.png
  → The top bar of the Addon Builder showing the "Back to List" link, status toggle (Published/Draft), and the Save/Update button
  → Highlight the status toggle switch

addon-builder-title.png
  → The Group Title text input at the top of the builder, with a descriptive name typed in (e.g. "Personalisation Options")

addon-builder-sidebar.png
  → The "Add Fields" sidebar panel showing the 2-column grid of field type buttons (Text Input, Textarea, Dropdown, Checkboxes, etc.)
  → All 12 field type buttons should be visible

addon-builder-assignment-rules.png
  → The Assignment Rules section inside the Addon Builder
  → Show the Visibility radio toggle (Global vs Targeted), the Reach search fields, and the Exceptions search fields

addon-builder-drag-reorder.png
  → A field row being actively dragged (use a mid-drag screenshot with the row slightly offset)
  → Alternatively: a static shot with the grip handle highlighted

addon-builder-field-collapsed.png
  → Multiple field rows in their default collapsed state, showing the field icon, label, type badge, and action buttons (expand, delete)

addon-builder-field-expanded.png
  → One field row in the expanded state showing the tab panel (General, Pricing, Stock, Conditions tabs)
  → General tab active, showing label, placeholder, description, required fields

## assignments/index.md
assignments-panel.png
  → The Assignment Rules section inside the Addon Builder
  → Show 3–4 rules: one Global, one Category include, one Product include, one Product exclude
  → The table/list with target type, target name, Include/Exclude toggle, priority, and remove button

assignments-add-rule.png
  → The Assignment Rules panel with the "Add Rule" button visible
  → Optionally: the target type dropdown open showing Global / Product / Category / Tag options

## admin/settings.md
settings-page.png
  → The full OptionBay Settings page
  → Show both sections: "General Settings" and "System & Maintenance"
  → All fields visible (orientation, font sizes, swatch sizes, debug toggle, delete data toggle)

## admin/orders.md
order-line-item.png
  → A WooCommerce order edit screen
  → One order line item with 2–3 OptionBay custom option rows visible below the product name
  → Example: "Engraving Text: Happy Birthday!" and "Gift Wrap?: Yes"

cart-item-options.png
  → The WooCommerce cart page
  → One product in the cart with 2–3 OptionBay option rows shown beneath the product name and image

## admin/export-import.md
export-panel.png
  → The Export panel expanded inline on the Option Groups page
  → Show the entity checkboxes (Option Groups ✓, Inventory Items ✓, Settings ✓) and "Download JSON" button

import-panel.png
  → The Import panel expanded inline
  → Show the JSON textarea (empty or with sample text) and the file upload button

## getting-started/installation.md
install-upload.png
  → WordPress admin "Add Plugins" screen with the "Upload Plugin" area visible
  → The "Choose File" button and file name input highlighted

## getting-started/quick-start.md
quick-start-result.png
  → A WooCommerce product page showing the final result of the Quick Start guide
  → Show an Engraving Text input and Gift Wrap radio buttons (No / Yes +$4.00) above the Add to Cart button

quick-start-step1.png
  → The Option Groups list with the "Add new group" button highlighted at the top of the page

quick-start-step3.png
  → The Addon Builder with two configured fields visible — a Text Input (Engraving Text) and Radio Buttons (Gift Wrap)
  → Both fields should be in expanded state showing their configuration

quick-start-step4.png
  → The Assignment Rules panel inside the Addon Builder
  → "Apply to specific products" is selected, and a product is chosen in the Reach search field

quick-start-step6.png
  → The frontend product page showing the Engraving Text input and Gift Wrap radio buttons rendered above the Add to Cart button
  → Show a realistic product with the OptionBay fields integrated naturally

## fields/choice/select.md
field-select-general.png
  → The Addon Builder backend showing the General settings for a Select Dropdown field
  → Highlight the Label, Description, and Placeholder inputs

field-select-validation.png
  → The Addon Builder backend showing the Validation settings for a Select Dropdown field
  → Highlight the "Field is Required" checkbox

field-select-options.png
  → The Addon Builder backend showing the Options block for a Select Dropdown field
  → Highlight the Add Option button and 2-3 row options (each with Label, Value, Price Type, Price Amount, Weight)

field-select-pricing.png
  → The Addon Builder backend showing the Pricing Logic settings for a Select Dropdown field
  → Highlight the field-level Price Type dropdown

field-select-conditions.png
  → The Addon Builder backend showing the Conditions tab for a Select Dropdown field
  → Highlight the Enable Conditional Logic toggle and condition rules

field-select-stock.png
  → The Addon Builder backend showing the Stock tab for a Select Dropdown field
  → Highlight the per-option stock linking table

field-select-frontend.png
  → A Select Dropdown field rendered on a WooCommerce product page
  → Show the native <select> element with the dropdown open showing 3–4 options
  → One option should have a price badge visible in the label (e.g. "Large (+$10.00)")

## fields/choice/radio.md
field-radio-general.png
  → The Addon Builder backend showing the General settings for a Radio Buttons field
  → Highlight the Label and Description inputs

field-radio-validation.png
  → The Addon Builder backend showing the Validation settings for a Radio Buttons field
  → Highlight the "Field is Required" checkbox

field-radio-options.png
  → The Addon Builder backend showing the Options block for a Radio Buttons field
  → Highlight the Add Option button and 2-3 row options (each with Label, Value, Price Type, Price Amount, Weight)

field-radio-pricing.png
  → The Addon Builder backend showing the Pricing Logic settings for a Radio Buttons field
  → Highlight the field-level Price Type dropdown

field-radio-conditions.png
  → The Addon Builder backend showing the Conditions tab for a Radio Buttons field
  → Highlight the Enable Conditional Logic toggle and condition rules

field-radio-stock.png
  → The Addon Builder backend showing the Stock tab for a Radio Buttons field
  → Highlight the per-option stock linking table

field-radio-frontend.png
  → A Radio Buttons field on a product page
  → Show 3 radio choices side by side or stacked, two with price badges
  → One radio is selected (highlighted / active state)

## fields/choice/checkbox.md
field-checkbox-general.png
  → The Addon Builder backend showing the General settings for a Single Checkbox field
  → Highlight the Label and Description inputs

field-checkbox-validation.png
  → The Addon Builder backend showing the Validation settings for a Single Checkbox field
  → Highlight the "Field is Required" checkbox

field-checkbox-pricing.png
  → The Addon Builder backend showing the Pricing Logic settings for a Single Checkbox field
  → Highlight the Price Type dropdown and Price Amount input

field-checkbox-conditions.png
  → The Addon Builder backend showing the Conditions tab for a Single Checkbox field
  → Highlight the Enable Conditional Logic toggle and condition rules

field-checkbox-stock.png
  → The Addon Builder backend showing the Stock tab for a Single Checkbox field
  → Highlight the Enable Stock Management toggle and Inventory Item selector

field-checkbox-frontend.png
  → A single checkbox toggle on a product page (no options, just one checkbox)
  → Show the label, checkbox, and price badge (e.g. "Add gift message? (+$2.00)")

## fields/choice/checkboxes.md
field-checkboxes-general.png
  → The Addon Builder backend showing the General settings for a Checkboxes field
  → Highlight the Label and Description inputs

field-checkboxes-validation.png
  → The Addon Builder backend showing the Validation settings for a Checkboxes field
  → Highlight the "Field is Required" checkbox

field-checkboxes-options.png
  → The Addon Builder backend showing the Options block for a Checkboxes field
  → Highlight the Add Option button and 2-3 row options (each with Label, Value, Price Type, Price Amount, Weight)

field-checkboxes-conditions.png
  → The Addon Builder backend showing the Conditions tab for a Checkboxes field
  → Highlight the Enable Conditional Logic toggle and condition rules

field-checkboxes-stock.png
  → The Addon Builder backend showing the Stock tab for a Checkboxes field
  → Highlight the Enable Stock Management toggle and Inventory Item selector

field-checkboxes-frontend.png
  → A multi-checkbox group on a product page
  → Show 3–4 checkboxes, each with its own label and price badge
  → Two are checked

## fields/choice/color-swatch.md
field-color-swatch-frontend.png
  → A Color Swatch field on a product page
  → Show 5–6 colored square/circle swatches
  → One is selected (showing a checkmark or highlight ring)
  → Price badge visible on at least one swatch label on hover or nearby

## fields/choice/image-swatch.md
field-image-swatch-frontend.png
  → An Image Swatch field on a product page
  → Show 4 small image thumbnails as swatch buttons
  → One is selected (highlighted border or checkmark overlay)

## fields/input/text.md
field-text-frontend.png
  → A Text Input field on a product page
  → Show the label, text input with placeholder text, and a description below it

field-text-general.png
  → The Addon Builder backend showing the General settings for a Text field
  → Highlight the Label, Description, and Placeholder inputs

field-text-validation.png
  → The Addon Builder backend showing the Validation settings for a Text field
  → Highlight the "Field is Required" checkbox

field-text-restrictions.png
  → The Addon Builder backend showing the Restrictions settings for a Text field
  → Highlight the Min Length and Max Length number inputs

field-text-pricing.png
  → The Addon Builder backend showing the Pricing Logic settings for a Text field
  → Highlight the Price Type dropdown and Price Amount (or Formula Expression) input

field-text-conditions.png
  → The Addon Builder backend showing the Conditions tab for a Text field
  → Highlight the Enable Conditional Logic toggle and condition rules

field-text-stock.png
  → The Addon Builder backend showing the Stock tab for a Text field
  → Highlight the Enable Stock Management toggle and Inventory Item selector

## fields/input/textarea.md
field-textarea-general.png
  → The Addon Builder backend showing the General settings for a Textarea field
  → Highlight the Label, Description, and Placeholder inputs

field-textarea-validation.png
  → The Addon Builder backend showing the Validation settings for a Textarea field
  → Highlight the "Field is Required" checkbox

field-textarea-restrictions.png
  → The Addon Builder backend showing the Restrictions settings for a Textarea field
  → Highlight the Min Length and Max Length number inputs

field-textarea-pricing.png
  → The Addon Builder backend showing the Pricing Logic settings for a Textarea field
  → Highlight the Price Type dropdown and Price Amount input

field-textarea-conditions.png
  → The Addon Builder backend showing the Conditions tab for a Textarea field
  → Highlight the Enable Conditional Logic toggle and condition rules

field-textarea-stock.png
  → The Addon Builder backend showing the Stock tab for a Textarea field
  → Highlight the Enable Stock Management toggle and Inventory Item selector

field-textarea-frontend.png
  → A Textarea field on a product page
  → Show the label and a multi-line textarea with placeholder
  → Description text visible below

## fields/input/number.md
field-number-general.png
  → The Addon Builder backend showing the General settings for a Number field
  → Highlight the Label, Description, and Placeholder inputs

field-number-validation.png
  → The Addon Builder backend showing the Validation settings for a Number field
  → Highlight the "Field is Required" checkbox

field-number-restrictions.png
  → The Addon Builder backend showing the Restrictions settings for a Number field
  → Highlight the Min Value, Max Value, and Step inputs

field-number-pricing.png
  → The Addon Builder backend showing the Pricing Logic settings for a Number field
  → Highlight the Price Type dropdown and Price Amount input

field-number-conditions.png
  → The Addon Builder backend showing the Conditions tab for a Number field
  → Highlight the Enable Conditional Logic toggle and condition rules

field-number-stock.png
  → The Addon Builder backend showing the Stock tab for a Number field
  → Highlight the Enable Stock Management toggle and Inventory Item selector

field-number-frontend.png
  → A Number input field on a product page
  → Show the label, number input with up/down spinners
  → Min/max description below the field

## fields/input/email.md
field-email-general.png
  → The Addon Builder backend showing the General settings for an Email field
  → Highlight the Label, Description, and Placeholder inputs

field-email-validation.png
  → The Addon Builder backend showing the Validation settings for an Email field
  → Highlight the "Field is Required" checkbox

field-email-pricing.png
  → The Addon Builder backend showing the Pricing Logic settings for an Email field
  → Highlight the Price Type dropdown and Price Amount input

field-email-conditions.png
  → The Addon Builder backend showing the Conditions tab for an Email field
  → Highlight the Enable Conditional Logic toggle and condition rules

field-email-stock.png
  → The Addon Builder backend showing the Stock tab for an Email field
  → Highlight the Enable Stock Management toggle and Inventory Item selector

field-email-frontend.png
  → An Email input field on a product page
  → Show the label and email input
  → Optionally show a browser validation tooltip or red border on an invalid entry

## fields/advanced/file-upload.md
field-file-general.png
  → The Addon Builder backend showing the General settings for a File Upload field
  → Highlight the Label and Description inputs

field-file-validation.png
  → The Addon Builder backend showing the Validation settings for a File Upload field
  → Highlight the "Field is Required" checkbox

field-file-restrictions.png
  → The Addon Builder backend showing the Restrictions settings for a File Upload field
  → Highlight the Allowed Types and Max File Size inputs

field-file-pricing.png
  → The Addon Builder backend showing the Pricing Logic settings for a File Upload field
  → Highlight the field-level Price Type dropdown

field-file-conditions.png
  → The Addon Builder backend showing the Conditions tab for a File Upload field
  → Highlight the Enable Conditional Logic toggle and condition rules

field-file-stock.png
  → The Addon Builder backend showing the Stock tab for a File Upload field
  → Highlight the Stock pool dropdown

field-file-frontend.png
  → A File Upload field on a product page
  → Show the label, native file input button, and the "Allowed: .jpg,.png,.pdf. Max size: 5 MB." hint text below

## fields/static.md
field-static-general.png
  → The Addon Builder backend showing the General settings for a Static Content field
  → Highlight the Admin Label and the HTML Content input box

field-static-conditions.png
  → The Addon Builder backend showing the Conditions tab for a Static Content field
  → Highlight the conditional rules

field-static-content-frontend.png
  → A Static Content field rendered on a product page
  → Show a styled heading and a short paragraph or notice banner above other fields

## pricing/index.md
pricing-tab.png
  → The Pricing tab open inside the Addon Builder field configuration panel
  → Show the Price Type dropdown open with options: None / Flat Fee / Percentage / Character Count / Math Formula
  → Amount input visible below

pricing-tab-flat.png
  → The Pricing tab with Price Type set to "Flat Fee" and a fixed amount (e.g. 5.00) filled in

pricing-tab-percentage.png
  → The Pricing tab with Price Type set to "Percentage" and a percentage amount (e.g. 10) filled in

pricing-tab-character.png
  → The Pricing tab with Price Type set to "Character Count" and a rate (e.g. 0.50) filled in

pricing-tab-formula.png
  → The Pricing tab with Price Type set to "Math Formula" and a dynamic formula expression (e.g. `[char_count] * 0.50`) filled in

pricing-option-level.png
  → The General tab of a Choice field (like a Select dropdown or Radio button group) showing the per-option configuration list
  → Highlight the Price Type and Price Amount fields inside the expanded accordion of one option

pricing-frontend-totals.png
  → The frontend product page showing how the selected option surcharges are summarized in the live totals block (e.g. "Options Total: +$13.00", "Product Price: $100.00", "Total Price: $113.00") above the add to cart button

## stocks/index.md
inventory-modal.png
  → The View Inventory modal fully open
  → Shows a table of stock items with name, stock count, backorders toggle, and edit/delete actions

inventory-modal-add.png
  → The "Add Item" form open inside the inventory modal showing Name, Stock Count, and Allow Backorders fields

inventory-modal-edit.png
  → The edit mode active for a single stock item row in the inventory modal table, with the inputs for Name and Stock Count highlighted
  → One item has a low count to illustrate the use case

## stocks/managing-items.md
inventory-modal-open.png
  → Same as inventory-modal.png — the modal opened from the Option Groups list (capture with the list visible in the background)
  → The "Add Item" button clearly visible at the top of the modal

inventory-modal-delete-confirm.png
  → A browser/UI confirmation dialog showing the warning when a user attempts to delete a stock item

inventory-create-inline.png
  → The Stock tab inside the Addon Builder
  → The inventory search dropdown open with a "Create new 'Engraving Slots'" option visible at the bottom of the results

## stocks/field-linking.md
stock-field-link.png
  → The Stock tab inside the Addon Builder for a text or number field
  → Show: Enable Stock toggle (on), inventory item selected ("Gift Wrap Sheets"), Reduction Mode dropdown

stock-field-link-reduction.png
  → The Stock tab with the Reduction Mode dropdown open, showing the options: Per Item Quantity / Per Line Item / Custom Formula

stock-field-link-formula.png
  → The Stock tab with the Reduction Mode set to "Custom Formula" and a custom formula expression (e.g. `qty * val`) entered in the formula field

stock-option-link.png
  → An individual choice option expanded inside a Radio or Select field
  → Show the stock sub-settings for that specific option: inventory item search and reduction mode

stock-frontend-outofstock.png
  → Frontend view of an option on a product page that is out of stock (e.g. a disabled radio button option or dropdown item with "(Out of stock)" next to it)

## visibility/index.md
visibility-tab.png
  → The Visibility tab open inside a field configuration in the Addon Builder
  → Show: Enable Conditional Logic toggle (on), Action dropdown ("Show this field when…"), Match Type (ALL), and 2 condition rules configured
  → Rules should be readable: e.g. Field = "Add Engraving?" | Operator = "equals" | Value = "1"

## fields/conditions.md
field-conditions-tab.png
  → The Conditions tab open inside an expanded field in the Addon Builder
  → Show the full conditions UI: Enable toggle (on), Action dropdown, Match Type, and 2 configured rules with field / operator / value columns visible
  → At least one rule should show a recognisable field name and value (e.g. "Add Engraving?" equals "1")
